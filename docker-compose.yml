services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: rounds
      POSTGRES_PASSWORD: rounds
      POSTGRES_DB: rounds
    ports:
      - "5433:5432"
    volumes:
      - rounds_db_data:/var/lib/postgresql/data

volumes:
  rounds_db_data:
